#
# Config file to start instances
#

# name of the key used to start instances
keypair = 'ec2_sydney'

# image to start
# original, can connect, userdata doesn't run?
#image = 'ami-d9d7f9ba'

# can't connect?
#image = 'ami-6c14310f'  # Ubuntu Server 14.04 LTS (HVM), SSD Volume Type

#
image = 'ami-dc361ebf'  # Amazon Linux AMI 2016.03.3 (HVM), SSD Volume Type

# worker node name prefix, number automatically appended
prefix = 'instance-{number:04d}'

# the default flavor of new instance(s)
flavour = 't2.micro'

# default security group(s) (may be ',' delimited)
secgroup = 'sydney'

# default region
region = 'ap-southeast-2'

# default zone
zone = 'ap-southeast-2b'
